name: PyPOWER

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  pypower:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12
    
    - name: Install packages
      run: |
        cd pypower
        python3 -m pip install --upgrade pip -r requirements.txt pylint

    - name: Analyse code
      run: |
        cd pypower
        pylint --fail-under=9 *.py

    - name: Run tests
      run: |
        cd pypower
        python3 tests.py || ( tar cfz test-results.tarz tests ; false )
        tar cfz test-results.tarz tests

    - name: Test results
      if: always()
      uses: actions/download-artifact@v5
      with:
        path: pypower/test-results.tarz
