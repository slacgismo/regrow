This folder contains the generation cost data used to scheduling.

To obtain the pypower 'gen' and 'gencost' data arrays, do the following:

    import gendata
    print(gendata.model("generation_cost.csv"))

Source: Jin Tan (NREL)

