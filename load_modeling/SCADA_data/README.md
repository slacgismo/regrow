Load data from an anonymized utility, stored is in MW, with each column as a
separate feeder. Timestamps are in PST/PDT (i.e., spring has a blank row and
fall is missing 1 one sample).

Temperature data is from NSRDB and stored in Celsius. Timestamps are UTC.
