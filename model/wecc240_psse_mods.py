from numpy import array
def wecc240_psse(case):

   # current the DC line has proxy load pair at CELILOCA and SYLMARLA
   # # dclines
   # case["dcline"] = array([
   #    [168, 38, 1, 2500.0, 2450.0, 0.0, 0.0, 1.01, 1.0, 0, 3100, -100, 100, -100, 100, 0, 0]
   # ])

   # case["dclinecost"] = array([
   #    [2,0.0,0.0,3,0.0,0.0,0.0]
   #    ])

   return case